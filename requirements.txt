akshare>=1.16.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
pyarrow>=12.0.0
openpyxl>=3.0.0
requests>=2.28.0
lxml>=4.9.0
# 锁定到 1.59.2：本地测试通过的版本，避免云端装到 API 不同的最新版再次翻车
# （历史教训：>=1.36.0 在云端被装到不支持 components.html(key=...) 的旧版，
#  以及回传非字符串值的版本，导致 TypeError）
streamlit==1.59.2
plotly>=5.18.0
baostock>=0.8.8
scikit-learn>=1.3.0

# Cloud Postgres 多用户持仓持久化（账号/持仓写 Supabase/Neon）
supabase>=2.0.0
psycopg2-binary>=2.9.9

# pg_store 优先 psycopg3(wheel 友好),fallback psycopg2
psycopg[binary]>=3.1.18
